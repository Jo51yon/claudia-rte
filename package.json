{
  "name": "@jo51yon/claudia-rte",
  "version": "1.1.0",
  "description": "Claudia shared rich-text and structured-evidence editor components (RichText, RichTextView, BlockEditor, BlockCompletion), ported from SafeSpaces and made project-agnostic via dependency injection for storage. See docs/_platform/component-library.md in the claudia repo.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "style.css"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0"
  }
}
